version: 2
updates:
  # Example for GitHub Actions (remove if not using Actions):
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  # Example for npm (if you use Node.js):
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
